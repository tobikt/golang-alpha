version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/query.sql"
    schema: "schemas/schema.sql"
    gen:
      go:
        package: "db"
        out: "../internal/db"
        sql_package: "pgx/v5"
